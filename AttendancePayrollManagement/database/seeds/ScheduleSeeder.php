<?php

use Illuminate\Database\Seeder;

class ScheduleSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // factory(App\Schedule::class)->create()->each(function ($schedule) {
        //     factory(App\Employee::class)->make(['schedule_id'=>$schedule->id]);
        // });
    }
}
